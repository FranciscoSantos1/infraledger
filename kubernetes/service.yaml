apiVersion: v1
kind: Service
metadata:
  name: infraledger-api-service
spec:
  type: NodePort
  selector:
    app: infraledger-api
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080