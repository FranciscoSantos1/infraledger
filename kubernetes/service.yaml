apiVersion: v1
kind: Service
metadata:
  name: infraledger-api-service
  labels:
    app: infraledger-api
spec:
  type: NodePort
  selector:
    app: infraledger-api
  ports:
    - name: http
      port: 5000
      targetPort: 5000
      nodePort: 30080