## Agent skills

### Issue tracker

Issues and specs are tracked as GitHub Issues (requires `git init` + a GitHub remote — not yet set up). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` at the repo root. See `docs/agents/domain.md`.
